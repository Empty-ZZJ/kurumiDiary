using UnityEngine;

public class clothesswitching : MonoBehaviour
{
    public Camera maincamera;
    public bool bool_ok;
    private float yl;

    // Start is called before the first frame update
    private void Start ()
    {
    }

    // Update is called once per frame

    private void Update ()
    {
    }
}